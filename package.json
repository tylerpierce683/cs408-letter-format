{
  "name": "web-dev",
  "version": "0.0.1",
  "description": "Learning to Code",
  "private": true,
  "license": "MIT",
  "author": "Cookie Monster",
  "scripts": {
    "start": "npx browser-sync start --server src --files src  --port 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10"
  }
}
